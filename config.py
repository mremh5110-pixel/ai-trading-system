"""
config.py
=========
الإعدادات المركزية للنظام. عدّل القيم هنا بدل ما تدور عليها داخل الأكواد.
"""

# ---------------------------------------------------------------------------
# الرموز والفريمات
# ---------------------------------------------------------------------------
SYMBOLS = ["XAUUSD", "EURUSD", "GBPUSD"]        # الرموز المراقبة
CORRELATION_SYMBOLS = {                          # رموز مرتبطة لتحليل العلاقات
    "XAUUSD": ["DXY", "US10Y", "EURUSD"],
}

TIMEFRAMES = ["M5", "M15", "H1", "H4", "D1"]     # الفريمات المستخدمة في MTF
PRIMARY_TIMEFRAME = "M15"                        # الفريم الأساسي لتوليد الإشارة
BARS_TO_FETCH = 500                              # عدد الشموع المطلوبة لكل طلب

# ---------------------------------------------------------------------------
# جلسات التداول (بتوقيت UTC)
# ---------------------------------------------------------------------------
SESSIONS_UTC = {
    "Asia":    (0, 8),
    "London":  (7, 16),
    "New York": (12, 21),
}

# ---------------------------------------------------------------------------
# إعدادات هيكل السوق / SMC
# ---------------------------------------------------------------------------
SWING_LOOKBACK = 3          # عدد الشموع يمين/يسار لاعتبار القمة/القاع swing point
EQUAL_LEVEL_TOLERANCE = 0.0006   # نسبة التسامح لاعتبار قمتين/قاعين "متساويين" (0.06%)
FVG_MIN_GAP_ATR_RATIO = 0.15     # أقل حجم فجوة (كنسبة من ATR) عشان تُحتسب FVG حقيقية

# ---------------------------------------------------------------------------
# إعدادات المؤشرات الكلاسيكية
# ---------------------------------------------------------------------------
RSI_PERIOD = 14
MACD_FAST, MACD_SLOW, MACD_SIGNAL = 12, 26, 9
ATR_PERIOD = 14
ADX_PERIOD = 14
BB_PERIOD, BB_STD = 20, 2
MA_PERIODS = [20, 50, 200]

# ---------------------------------------------------------------------------
# إدارة المخاطر / توليد الأهداف
# ---------------------------------------------------------------------------
DEFAULT_RISK_REWARD_TARGETS = [1.0, 2.0, 3.0]   # TP1, TP2, TP3 كمضاعفات من المخاطرة
STRONG_SETUP_EXTRA_TARGET_RR = 4.5              # TP4 يُضاف فقط للصفقات القوية جدًا
MIN_CONFLUENCE_SCORE_TO_TRADE = 60              # الحد الأدنى (من 100) لاعتبار الصفقة قابلة للتنفيذ

# ---------------------------------------------------------------------------
# التعلم الذاتي
# ---------------------------------------------------------------------------
TRADE_LOG_PATH = "data/trade_log.csv"
MODEL_PATH = "data/self_learning_model.joblib"
MIN_TRADES_BEFORE_TRAINING = 30     # أقل عدد صفقات مسجّلة قبل أول تدريب فعلي
RETRAIN_EVERY_N_TRADES = 10

# ---------------------------------------------------------------------------
# مسارات الإخراج (تُستخدم بين main.py ولوحة Streamlit)
# ---------------------------------------------------------------------------
LATEST_SIGNAL_PATH = "data/latest_signal.json"
